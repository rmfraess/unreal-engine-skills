# UE 5.8.2 P1 implementation ledger

- **Scope:** approved P1 implementation and parent review complete across the audited skill tree. Skills were edited sequentially, including supporting references and review corrections. Pre-existing working-tree changes are preserved.
- **Authority:** installed `C:/Program Files/Epic Games/UE_5.8/Engine/Build/Build.version` (`5.8.2`, CL `56702186`, `++UE5+Release-5.8`).
- **Policy:** only verified P1 corrections were applied; P2/P3 additions remain deferred. Unsupported audit premises were rejected or narrowed, not copied blindly. The audit report remains unchanged. A repository-local Node 22 toolchain and disposable `evals/fixtures/SkillAuditP1` project support validation; no production project was modified. Implementation acceptance preceded the separately approved local-commit closeout below; pushing remains unauthorized.
- **Inventory:** report parsing found exactly `117` P1 findings across `49` skills. The ledger has `117` unique rows with `missing=0`, `extra=0`, and `duplicates=0`: `107` **Applied**, `6` **Corrected-to-report**, `2` **Corrected**, `1` **Rejected** (`EDITOR-PYTHON-01`), and `1` **Applied (P1 constructor only)** (`UMG-02`; its audit P2 hierarchy cleanup was intentionally not applied). The final interval contains `24` P1 IDs and all `24` are reviewed/applied; no P1 ID is missing or unresolved.

## Final acceptance evidence

- **Full-tree static checks:** explicit Node `v22.23.2`; all `62` skills passed cached `skills-ref@0.1.5`; Hermes compatibility passed; core citations checked `587` distinct existing engine paths across `203` Markdown files; `git diff --check` passed. The combined command records each exit code: `66` checks, `0` failures. Details are in ignored `evals/fixtures/SkillAuditP1/Saved/StaticValidation.json`. Citation existence is not API or runtime proof; vendor guidance was checked against vendor 9.5 documentation, not the core citation checker.
- **Executable checks:** `UE_ENGINE_ROOT='C:/Program Files/Epic Games/UE_5.8' python evals/fixtures/SkillAuditP1/run.py` builds with UHT/UBT and starts a fresh headless editor. Final run: `Result: Succeeded`, process exit `0`, `SkillAudit.P1.CoreContracts` succeeded, `0` failures and `0` warnings. The runner deletes the old JSON report before launching. Source and scope are documented in the fixture README; logs and the fresh JSON report are under its ignored `Saved/` directory.
- **Coverage reconciliation:** independently parsed `117` audit P1 IDs and matched `117` unique ledger rows across `49` skills, with no missing, extra, or duplicate IDs. Of these, `116` were applied/corrected (including the P1-only part of `UMG-02`), and `EDITOR-PYTHON-01` was rejected after both `CallInEditor` spellings passed UHT and metadata checks.
- **Final review corrections:** verified the previously failed UMG edits in the actual diff; no placeholder file exists. Corrected UMG lifecycle wording/backticks, preserved intentionally external UDS lights, removed remaining native-looking UDW call spellings, and reconciled save/load sibling guidance. For `SAVE-03`, generic memory/slot loading requires explicit migration rather than a `PostLoad` override. For `SAVE-05`, checked the binary loader's cooked/archive gates rather than relying on the text loader or editor behavior; the stock loader does not set `ArIsSaveGame`.
- **Limits:** the probe is a representative CPU/reflection/API check, not compilation of every snippet. No rendering, multiplayer, cook/package/device matrix, physical save-slot I/O, migration corpus, or UDS/UDW asset/Blueprint validation was performed. UDS/UDW 9.5 assets are not installed here. Build tools may use their normal system caches. P2/P3 work and unrelated pre-existing changes are outside this acceptance.

## Local-commit closeout

The pre-P1 diff was recovered from the audit checkpoint: seven modified owner skills and
the untracked Windows PIE reference. Those edits remain outside this commit. The isolated
P1 version of the landscape owner retains only a minimal resolved-weight API correction
for `LAND-03`, rather than importing the pre-existing target-layer and spline procedures.
The working files retain those procedures unchanged. The audit is included unchanged as
the ledger's proposal reference; it is not an instruction to apply deferred findings.

Closeout reran all 66 working-tree static checks successfully and the fresh editor probe
reported one success, zero failures and zero warnings (`2026.09.19-03.52.55` UTC).
An isolated export of the index also passed Hermes compatibility, all 62 skill validators,
and the core citation check (587 paths). Its fixture sources match the executed fixture.
The staged diff check passes excluding the unchanged audit report, whose pre-existing
Markdown hard breaks and quoted diff blank lines trigger trailing-whitespace diagnostics.
The report was preserved rather than reformatted to hide those diagnostics.
GitHub Issues is disabled on the routed repository; follow-up ticket publication requires
a tracker decision and approval of the breakdown.

## Findings

Entries are appended after each skill is reconciled and patched. “Verification limits” records what was not exercised beyond source/static inspection.

### core/actors-and-components

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| A&C-01 | **Applied** | `skills/core/actors-and-components/SKILL.md`; `references/actor-lifecycle.md`; `references/components-and-registration.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Private/Actor.cpp:4808-4848` calls `BeginPlay()` on registered components before `ReceiveBeginPlay()`; `Engine/Source/Runtime/Engine/Private/Components/ActorComponent.cpp:1648-1662` implements component `BeginPlay`; `Engine/Source/Runtime/Engine/Classes/Components/ActorComponent.h:930-943` documents the component Blueprint event ordering. | Source/static verification only; no PIE, network-spawn, streaming-resurrection, or packaged runtime lifecycle test. Searched the full skill directory for sibling actor/component BeginPlay-order passages; only the three patched owner/reference passages matched the stale wording. |

### core/animation-system

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|


### core/audio-and-metasounds

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| AUDIO-02 | **Applied** | `skills/core/audio-and-metasounds/references/mixing-classes-submixes-concurrency.md` | Installed UE 5.8.2 `Engine/Source/Runtime/AudioMixer/Public/AudioMixerBlueprintLibrary.h:238-240` declares `UAudioMixerBlueprintLibrary::AddSubmixEffect`; a bounded source search under `Engine/Source` found no `AddSoundToMix` symbol. | Parent's UE 5.8.2 fixture compile-checked the `AddSubmixEffect` signature and passed UBT; no audio-device/submix playback test or project module compile. Searched the full audio skill for `AddSoundToMix`; the patched reference was the only stale occurrence. |
| AUDIO-03 | **Applied** | `skills/core/audio-and-metasounds/SKILL.md` | Installed UE 5.8.2 `Engine/Plugins/Runtime/Metasound/Source/MetasoundEngine/Public/MetasoundSource.h:320-326` documents and declares `GetGeneratorForAudioComponent` as `TWeakPtr<Metasound::FMetasoundGenerator>`; the sibling `references/metasound-parameters-and-builder.md:61-86` already uses `Pin()` and identifies `UMetasoundGeneratorHandle` as a Blueprint wrapper. | No audio playback or generator-lifetime runtime test; output access still depends on an active generator and project/plugin configuration. Searched sibling references and retained the already-correct weak-pointer example. `AUDIO-01` remains P2 and was not changed. |

### core/automation-and-testing

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| TEST-01 | **Applied** | `skills/core/automation-and-testing/SKILL.md`; `references/functional-tests.md` | Installed UE 5.8.2 `Engine/Source/Developer/FunctionalTesting/Classes/FunctionalTest.h:391,731-734` declares `AutoDestroyActors` and `AFunctionalTest::RegisterAutoDestroyActor(AActor*)`; source search found no `AddActorToDestroyOnTestEnd`. | Parent's UE 5.8.2 fixture compile-checked the `RegisterAutoDestroyActor` signature and passed UBT; no functional-test map, editor, cleanup, or automation runtime run. Searched the full skill; the old helper is gone from executable examples/prose, while the old name is not retained as a compatibility claim. |
| TEST-02 | **Applied** | `skills/core/automation-and-testing/SKILL.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Core/Public/Misc/AutomationTest.h:224-225,2014-2020` declares `AddError`, `AddWarning`, and `TestEqualSensitive`; no `AddErrorS`, `AddWarningS`, or `TestEqualInsensitive` matches. `Engine/Source/Runtime/Core/Private/Misc/AutomationTest.cpp:2161-2192` implements string `TestEqual` case-insensitively, while `:2293-2323` implements `TestEqualSensitive`. | Parent's UE 5.8.2 fixture runtime case passed the string `TestEqual` ignore-case check; no broader project automation execution. The legacy names remain only in the gotcha as explicit unavailable names, not as callable/deprecated APIs. `TEST-03` remains P2 and was not changed. |

### core/blueprint-cpp-integration

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| BP-CPP-02 | **Applied** | `skills/core/blueprint-cpp-integration/SKILL.md` | Installed UE 5.8.2 `Engine/Source/Runtime/CoreUObject/Public/UObject/Interface.h:17-26` confirms the reflected/native interface pair; `ObjectMacros.h:1350-1351` declares `AllowPrivateAccess`. The existing reference files were read and already contain the generated `Execute_` dispatch and private-property rules; the main skill now states both boundary requirements together. | No project UHT/Blueprint compile or generated dispatcher execution; source/static verification only. Searched all four skill files for sibling interface/private-property guidance and changed only the missing owner passage. `BP-CPP-01` remains P2 and was not changed. |

### core/blueprint-fundamentals

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| BP-FUND-01 | **Applied** | `skills/core/blueprint-fundamentals/SKILL.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Private/Actor.cpp:274-282` initializes `PrimaryActorTick.bCanEverTick = false`; `Engine/Source/Editor/KismetCompiler/Private/KismetCompiler.cpp:5504-5610` shows non-empty Blueprint Tick plus `bCanBlueprintsTickByDefault`, universal `AActor`/`UActorComponent` parent, and `ChildCanTick` conditions. | No Blueprint asset compile, PIE, or project-setting test; source/compiler-path verification only. Searched all three references for the stale “Blueprint ticks by default” claim; no sibling copy required patching. |
| BP-FUND-02 | **Applied** | `skills/core/blueprint-fundamentals/SKILL.md`; `references/graphs-variables-and-components.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/GameFramework/Actor.h:3439-3445` declares `ExecuteConstruction` and `OnConstruction`; the reference’s `USimpleConstructionScript::ExecuteScriptOnActor` path and rerun model were read and retained with explicit idempotence/context guards. | No editor transaction, construction-spawn, World Partition, Blueprint compile, or PIE/cooked runtime test; source/static verification only. Searched the skill directory for construction-script world/spawn absolutes and patched the two affected owner/reference passages. `BP-FUND` P2/P3 findings were not applied. |

### core/character-and-movement

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| CHAR-MOVE-01 | **Applied** | `skills/core/character-and-movement/SKILL.md`; `skills/core/character-and-movement/references/networked-movement.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/Interfaces/MovementBaseInterface.h:11-71` declares `FMovementBaseInterfaceData`; `Engine/Source/Runtime/Engine/Classes/GameFramework/CharacterMovementComponent.h:904-910,1608-1614,1854-1861` marks `UPrimitiveComponent*` movement-base APIs deprecated and provides interface-data replacements; `Engine/Source/Runtime/Engine/Classes/GameFramework/Character.h:89-96,146-202,514-519` shows the owner representation and new utilities; `Engine/Source/Runtime/Engine/Private/Character.cpp:1658-1688` converts replicated base data and calls `SetBase` to establish dependencies and notifications. | No custom CMC compile, networked PIE, or deprecation-as-error check; source/static verification only. Read all four skill files and searched the skill directory for sibling movement-base passages; only the owner bullet and network Version notes required the migration warning. |

### core/coding-standards

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| CODING-01 | **Applied** | `skills/core/coding-standards/SKILL.md`; `skills/core/coding-standards/references/formatting-and-includes.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Core/Public/Misc/AssertionMacros.h:175-177` documents that `check` expressions are evaluated only when enabled and `verify` expressions are always evaluated; `:221-230` defines `check` under `DO_CHECK`; `:317-327` shows the disabled branch; `:343-365,461-472` documents non-fatal `ensure` behavior and expression-only fallback. | No shipping/development build matrix or assertion runtime test; source/static verification only. Read all three references; `reflection-and-uht.md:185-195` contains no assertion guidance to update, so no unsupported cross-reference was added. Parent's UE fixture is representative and does not exercise check/ensure configuration behavior. |

### core/control-rig-and-ik

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| RIG-01 | **Applied** | `skills/core/control-rig-and-ik/SKILL.md`; `skills/core/control-rig-and-ik/references/control-rig-graph.md` | Installed UE 5.8.2 `Engine/Plugins/Animation/ControlRig/Source/ControlRig/Public/ControlRig.h:123-127` declares `UControlRig::GetRigModulePrefix`; a literal search across the installed `Engine/Source` found no `GetRigModuleNameSpace` declaration or use. | No Control Rig editor/tooling compile or modular-rig runtime test; source/static verification only. Searched all three actual references and the owner skill for the absent symbol; retained it only in explicit “not present” migration warnings. |
| RIG-02 | **Applied** | `skills/core/control-rig-and-ik/SKILL.md`; `skills/core/control-rig-and-ik/references/animgraph-ik-nodes.md` | Installed UE 5.8.2 `Engine/Plugins/Animation/ControlRig/Source/ControlRig/Public/AnimNode_ControlRig.h:30-34` marks `GetControlRigClass()` deprecated and exposes `GetControlRigAssetReference()`; `:48-51` retains `SetControlRigClass()` and `:60-76` declares `FControlRigAssetStrongReference` plus deprecated class fields. | No AnimBP asset compile/editor node serialization or runtime evaluation test. The audit’s old-side `## Control Rig node` context was absent from the actual reference; added the verified warning at the reference introduction rather than mechanically applying the stale insertion context. Searched all actual sibling files for class-only guidance. |

### core/core-types-and-containers

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| TYPES-01 | **Applied** | `skills/core/core-types-and-containers/SKILL.md`; `skills/core/core-types-and-containers/references/math-types.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Core/Public/Math/Quat.h:205-214,219-225` documents `C = A * B` as applying `B` first then `A`; `Engine/Source/Runtime/Core/Public/Math/TransformVectorized.h:54-58` explicitly documents the opposite transform composition order. | Parent's UE 5.8.2 fixture runtime quaternion-order case and wrong-order control passed; no gameplay compile or broader math/runtime test. Read all four references and searched the full skill for sibling quaternion-order wording; only the two cited passages contained the reversed contract. |

### core/cpp-fundamentals

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| CPP-01 | **Applied** | `skills/core/cpp-fundamentals/SKILL.md`; `skills/core/cpp-fundamentals/references/reflection-macros.md` | Installed UE 5.8.2 `Engine/Source/Runtime/CoreUObject/Private/UObject/GarbageCollection.cpp:6953-6975` implements `FStructProperty::EmitReferenceInfo`: it detects nested properties with strong UObject references and emits their reference schema. | Parent's read-only UE 5.8.2 fixture `evals/fixtures/SkillAuditP1/run.py` executed with `UE_ENGINE_ROOT` and reported `UBT Result:Succeeded`, editor exit `0`, and fresh `Saved/Report/index.json` `succeeded=1 failed=0 warnings=0`; its nested reflected UObject case survived GC while referenced and was collected after clearing the reference. No independent fixture was created; fixture coverage is representative, not exhaustive for arbitrary struct traits/custom serialization or all GC modes. Read all three actual references and searched sibling wording; only the owner explanation and reflection reference contained the stale blanket statement. |

### core/debugging-techniques

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| DEBUG-01 | **Applied** | `skills/core/debugging-techniques/SKILL.md`; `skills/core/debugging-techniques/references/draw-debug-and-console.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Public/DrawDebugHelpers.h:13-17,228-232` declares DrawDebug APIs only under `ENABLE_DRAW_DEBUG` and its disabled branch says calls cause a compile error; `Engine/Source/Runtime/Engine/Private/UnrealEngine.cpp:12466-12470` keeps `AddOnScreenDebugMessage` declared but gates its implementation on `UE_BUILD_SHIPPING || UE_BUILD_TEST`. | No Shipping/Test compile or packaged runtime test; source/static verification only. Read all three references and searched the skill for stale “no-op/no manual guard” claims; only the two owner passages and draw-debug introduction required changes. |

| DEBUG-03 | **Applied** | `skills/core/debugging-techniques/references/gameplay-debugger.md` | Installed UE 5.8.2 `Engine/Source/Runtime/GameplayDebugger/Public/GameplayDebuggerAddonBase.h:43-78` requires `FName KeyName` for both `BindKeyPress` overloads; `GameplayDebuggerTypes.h:360-381` declares `FGameplayDebuggerInputModifier`. | No Gameplay Debugger module compile or key-input PIE test; source/static verification only. Searched the full skill for `EKeys::Q`; the corrected sample is the only key-binding occurrence. |

### core/delegates-and-events

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| DELEGATE-01 | **Applied** | `skills/core/delegates-and-events/references/binding-and-lifetime.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Core/Public/Delegates/IDelegateInstance.h:13-35` defines `FDelegateHandle` as a native class handle with an unset ID; it is not a reflected property type. | Parent's UE 5.8.2 fixture UHT-checked an ordinary `FDelegateHandle` member and passed; no UHT check of every delegate binding lifetime or runtime unbind path. Read all three references and searched the skill for `UPROPERTY` adjacent to `FDelegateHandle`; the malformed example was the only occurrence and is now an ordinary member. |

### core/editor-scripting-and-python

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|

| EDITOR-PYTHON-02 | **Applied** | `skills/core/editor-scripting-and-python/references/python-api.md` | Installed UE 5.8.2 `Engine/Plugins/Experimental/PythonScriptPlugin/Source/PythonScriptPlugin/Private/PyGenUtil.cpp:3353-3365` maps `FStrProperty` to `str`, `FNameProperty` to `Name`, and `FTextProperty` to `Text`; `PyConversion.cpp:761-763` registers native conversion handlers for all three property types. | No generated-stub inspection or Python editor runtime test; source/static verification only. Read all three references and searched sibling type-mapping passages; the single combined `str` row was the only stale mapping. Parent's UE fixture did not exercise Python wrapper typing. |

### core/enhanced-input

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| ENHANCED-INPUT-01 | **Applied** | `skills/core/enhanced-input/SKILL.md`; `skills/core/enhanced-input/references/actions-and-contexts.md` | Installed UE 5.8.2 `Engine/Plugins/EnhancedInput/Source/EnhancedInput/Public/InputActionValue.h:102-124` documents that `Get<T>()` has no value-type check and exposes `GetValueType()`; `:203-227` implements `Get<bool>()` with `IsNonZero()` and vector getters as component reads. | Source/static verification only; no project input-asset, remapping, PIE, or packaged runtime test. Read all four skill files and searched matching value-type passages; the owner guidance, owner gotcha, and actions reference were the only stale claims. Node `v22.23.2`: Hermes compatibility, `skills-ref@0.1.5`, and core citation validation all passed (62 skills, 4 Markdown files, 11 citations). |

### core/game-thread-performance

No P1 finding. The audit's no-change disposition was retained after reading `SKILL.md`; the generic profiling, game-thread ownership, and worker-to-game-thread handoff guidance remains source-compatible with installed UE 5.8.2. No skill file was edited.

### core/gameplay-ability-system

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| GAS-01 | **Applied** | `skills/core/gameplay-ability-system/references/gameplay-abilities.md`; `skills/core/gameplay-ability-system/references/ability-system-component.md` | Installed UE 5.8.2 `AbilitySystemComponent_Abilities.cpp:2496-2527` resolves event tags/parents, copies the payload, sets `EventTag`, and calls `InternalTryActivateAbility`; `:1704-1705,1812-1827` documents the shared activation, `CanActivateAbility`, instancing, networking, and prediction path. | No GAS project compile, ability asset, event-driven PIE, prediction, or replication run. Read all five skill files and searched the complete skill for the stale event-bypass wording; the two reference passages were the only matches. `skills-ref@0.1.5` and core citation validation passed under Node `v22.23.2` (5 Markdown files, 13 citations). |
| GAS-03 | **Applied** | `skills/core/gameplay-ability-system/SKILL.md` | Installed UE 5.8.2 `ActiveGameplayEffectHandle.h:21-38` deprecates the integer constructor and provides `GenerateNewHandle`; `:40-48` defines the invalid Instant sentinel and undefined owner; `:62-70` documents null ownership after removal and deprecated global-map helpers; `:99-108` stores a weak ASC owner. | No custom handle integration compile or active-effect lifetime test. Source/release-note contract only; the new guidance deliberately does not promise an ASC for an Instant or removed handle. |
| GAS-04 | **Applied** | `skills/core/gameplay-ability-system/SKILL.md` | Installed UE 5.8.2 `AbilitySystemGlobals.h:63-72` routes `Get()` to the module singleton and documents first-use initialization; `GameplayAbilitiesModule.cpp:24-43` lazily creates, roots, and initializes it; `AbilitySystemGlobals.cpp:63-70` makes repeated initialization idempotent. | No project startup, configured-class failure, prediction, or montage/target-data runtime test. Source/static verification only; explicit calls remain possible but are documented as redundant rather than forbidden. |
| GAS-05 | **Applied** | `skills/core/gameplay-ability-system/SKILL.md`; `skills/core/gameplay-ability-system/references/gameplay-abilities.md` | Installed UE 5.8.2 `GameplayAbilityTypes.h:36-55` still declares `NonInstanced` with `UE_DEPRECATED_FORGAME(5.5)`; `GameplayAbility.cpp:34-35,109-117` exposes `AbilitySystem.Fix.AllowNonInstancedAbilities` and maps the deprecated policy to `InstancedPerActor` unless enabled. | No ability asset validation, compatibility-CVar, state, RPC, or replication test. The reference row remains a deprecation/avoidance recommendation and no removal claim is retained. |

### core/gameplay-architecture-planning

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| ARCH-01 | **Applied** | `skills/core/gameplay-architecture-planning/SKILL.md`; `skills/core/gameplay-architecture-planning/references/decision-matrices.md` | Installed UE 5.8.2 source scan found no `UGameplayMessageSubsystem`, `GameplayMessageSubsystem`, or `FGameplayMessage`. `Engine/Plugins/Experimental/AsyncMessageSystem/AsyncMessageSystem.uplugin:12-22` sets `EnabledByDefault=false` and `IsExperimentalVersion=true`; `Source/AsyncMessageSystem/Public/AsyncGameplayMessageSystem.h:14-30` exposes `FAsyncGameplayMessageSystem`. | No project plugin enablement, Build.cs integration, message dispatch, tick-group, thread-safety, or shutdown test. Read both skill files; all generic owner/matrix message-bus passages were qualified, and the experimental option is explicitly deferred to project validation. Node `v22.3.2` `skills-ref@0.1.5` and core citation validation passed (2 Markdown files, 21 citations). |

### core/gameplay-framework

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| FRAMEWORK-01 | **Applied** | `skills/core/gameplay-framework/references/gamemode-and-state.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/GameFramework/GameMode.h:67-69` declares `AGameMode::MatchState` as transient; `GameState.h:33-35,59-61` declares the replicated `AGameState::MatchState` and `OnRep_MatchState`. | No multiplayer PIE, dedicated-server, seamless-travel, or client replication run. Read all four skill files; the deep reference’s replicated-GameMode claim was the only P1-stale passage, while the owner summary was already directionally correct. Node `v22.23.2` `skills-ref@0.1.5` and core citation validation passed (4 Markdown files, 13 citations). |

### core/gameplay-tags

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| TAGS-01 | **Corrected-to-report** | `skills/core/gameplay-tags/SKILL.md`; `skills/core/gameplay-tags/references/native-tags.md` | Follow-up correction to the earlier entry: installed UE 5.8.2 `Engine/Source/Runtime/GameplayTags/Public/NativeGameplayTags.h:36-46,53-65` constructs macro tags as module static objects and enforces `.cpp` file scope; `Classes/GameplayTagsManager.h:395-429` documents late `AddNativeGameplayTag`, `FastReplication` invalidation, `DoneAddingNativeTags`, and startup delegates. The reference no longer promises a universal before-`main()` ordering; it scopes the timing to module static initialization and retains the late-registration warning. | No project tag dictionary, module load-order, fast-replication parity, late-registration, or packaged network run. All four skill files were reread and native-registration/FastReplication sibling passages searched. Node `v22.23.2` compatibility, scoped `skills-ref@0.1.5`, citations (4 Markdown files, 7 citations), and scoped `git diff --check` passed. |

### core/importing-content

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| IMPORT-01 | **Applied** | `skills/core/importing-content/SKILL.md`; `skills/core/importing-content/references/interchange-framework.md` | Installed UE 5.8.2 `Engine/Plugins/Interchange/Extensions/OpenUSD/InterchangeOpenUSD.uplugin:13-17,23-33` marks the plugin disabled by default, experimental, and supplies `InterchangeOpenUSDImport` plus `InterchangeOpenUSDEditor`; `Source/Import/Public/InterchangeUsdTranslator.h:171-197` declares `UInterchangeUSDTranslator`; `InterchangeUSDPipeline.h:13-16` declares `UInterchangeUsdPipeline`. `InterchangeUsdTranslator.cpp:59-70` enables USD asset support by default but defaults `Interchange.FeatureFlags.Import.USD.ToLevel` to false. Epic's version-matched UE 5.8 release notes classify USD asset import as production ready, level import as experimental, and USD Pregen as experimental; those classifications are kept as release-note guidance rather than inferred from the descriptor alone. | No USD SDK availability, project plugin enablement, USD asset/level import, Pregen, or packaged cook run. Installed source does not establish product/release-note readiness or Pregen behavior; the classifications above came from the version-matched release notes and the implementation guidance remains subject to project validation. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and core citation validation passed (4 Markdown files, 20 citations). |
| IMPORT-02 | **Applied** | `skills/core/importing-content/SKILL.md`; `skills/core/importing-content/references/reimport-and-import-data.md` | Installed UE 5.8.2 `Engine/Plugins/Interchange/Runtime/Interchange.uplugin` defines the runtime plugin separately from `Engine/Plugins/Interchange/Editor/InterchangeEditor.uplugin`; `Engine/Source/Runtime/Interchange/Engine/Public/InterchangeManager.h:647-694` exposes synchronous and asynchronous import APIs. | No packaged runtime import or plugin/cook smoke test; editor-only module availability was checked statically. Existing non-Interchange import paths and format-specific plugin requirements still need project-specific validation. |

### core/landscape-and-foliage

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| LAND-01 | **Corrected-to-report** | `skills/core/landscape-and-foliage/SKILL.md`; `skills/core/landscape-and-foliage/references/landscape.md` | Follow-up correction to the earlier entry: installed UE 5.8.2 `Engine/Source/Runtime/Landscape/Classes/LandscapeProxy.h:924-930` documents `NumSubsections` and requires `SubsectionSizeQuads + 1` to be a power-of-two vertex dimension; `LandscapeEdit.cpp:140-151` enforces `NumSubsections * SubsectionSizeQuads == ComponentSizeQuads`. The reference now states that 63 and 127 are valid section-quad values with either 1 × 1 or 2 × 2 subsection layouts, rather than presenting 2 × 2 as exclusive; the 32 × 32, 2 × 2 examples remain 4033 and 8129 vertices. | Python arithmetic check returned 4033 and 8129; parent reran the Node22/version, Hermes, scoped skills-ref, scoped citation, and diff checks. No editor heightmap import/create, save/reopen, or visual landscape validation. Existing target-layer, spline, and persistence edits were preserved. |
| LAND-02 | **Applied** | `skills/core/landscape-and-foliage/SKILL.md`; `skills/core/landscape-and-foliage/references/landscape.md` | Installed UE 5.8.2 `LandscapeProxy.h:489-503,691-696` declares `bEnableNanite`, the deprecated singular `NaniteComponent`, and the live `NaniteComponents` array. | No Nanite landscape build, render, platform fallback, or asset migration run. |
| LAND-03 | **Applied** | `skills/core/landscape-and-foliage/references/landscape.md` | Installed UE 5.8.2 `LandscapeComponent.h:1262` declares `ULandscapeComponent::GetLayerWeightAtLocation`; `LandscapeInfo.h:188-191` marks `XYtoComponentMap` editor-only and keeps `XYtoCollisionComponentMap` valid; `LandscapeProxy.h:1101-1102` exposes runtime height/physical-material queries. | No editor component lookup, runtime bake, collision trace, or packaged query run. The owner’s already-corrected resolved-weight guidance and pre-existing landscape edits were left intact. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and core citation validation passed (4 Markdown files, 21 citations). |

### core/levels-and-world-partition

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| WP-01 | **Applied** | `skills/core/levels-and-world-partition/references/data-layers-and-hlod.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Public/WorldPartition/HLOD/HLODRuntimeSubsystem.h:53-65` exposes `OnCellShown`, `OnCellHidden`, and `GetHLODObjectsForCell`; `:113-128` stores loaded HLOD objects keyed by runtime cell. | No World Partition map, HLOD build, cell streaming, memory capture, or runtime hash/visibility run. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and core citation validation passed (4 Markdown files, 13 citations). |

### core/lighting-and-lumen

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| LIGHT-01 | **Applied** | `skills/core/lighting-and-lumen/SKILL.md`; `skills/core/lighting-and-lumen/references/light-components-and-mobility.md` | Epic's UE 5.8 release notes classify MegaLights as entering Production Ready and Lumen Lite as Beta, with Lumen Lite using irradiance fields with probe occlusion and becoming the current-generation handheld-console default. Installed UE 5.8.2 `LightComponent.h:166-179` exposes `bAllowMegaLights` and `MegaLightsShadowMethod`; `LumenIrradianceFieldGather.cpp:88-108` exposes the probe atlas/occlusion implementation symbols. | No target scalability profile, GPU, editor visualization, PIE, packaged build, or performance capture. Release-note status was not treated as a promise of target-specific quality. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and core citation validation passed (4 Markdown files, 10 citations). |
| LIGHT-03 | **Applied** | `skills/core/lighting-and-lumen/SKILL.md`; `skills/core/lighting-and-lumen/references/lumen-gi-and-reflections.md` | Version-matched Lumen technical documentation states Software Ray Tracing requires Shader Model 6 and mesh distance fields; installed UE 5.8.2 `RenderUtils.h:644-649` exposes Lumen platform-support checks. | No target RHI/GPU, project settings, PIE, packaged build, or Lumen visualization run. The source does not itself establish every platform matrix detail; the guidance retains the version-matched documentation qualification. |

### core/logging-and-assertions

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| LOG-ASSERT-01 | **Applied** | `skills/core/logging-and-assertions/references/assertions.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Core/Public/Misc/Build.h:205-212` defaults `USE_CHECKS_IN_SHIPPING` to 0 and derives `USE_ENSURES_IN_SHIPPING` from it; the assertion reference now states the explicit opt-in. | No Debug/Development/Test/Shipping target was compiled and no shipping crash-report path was exercised. |
| LOG-ASSERT-02 | **Applied** | `skills/core/logging-and-assertions/SKILL.md`; `skills/core/logging-and-assertions/references/structured-logging.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Core/Public/Logging/StructuredLog.h:196-204` deprecates `GetFormat`/`SetFormat` and provides `GetUtf8Format`/`SetUtf8Format`. | No custom output device consumed a structured record; UTF-8 lifetime/encoding and downstream serialization remain source-reviewed only. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and core citation validation passed (4 Markdown files, 10 citations). |

### core/materials-and-shaders

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| MAT-01 | **Applied** | `skills/core/materials-and-shaders/references/material-cpp-and-collections.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/Components/PrimitiveComponent.h:1183-1185` declares `SetCustomPrimitiveDataFloat(int32 DataIndex, float Value)`; `InstancedStaticMeshComponent.h:321-331` declares the per-instance `SetCustomDataValue(InstanceIndex, CustomDataIndex, CustomDataValue, ...)`, `SetNumCustomDataFloats`, and `SetCustomData`. | No material asset compile, ISM/HISM render, custom-data buffer, or runtime visual test. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and core citation validation passed (4 Markdown files, 15 citations). |

### core/memory-and-gc

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| MEMORY-GC-01 | **Corrected-to-report** | `skills/core/memory-and-gc/references/object-pointer-types.md` | Follow-up correction to the earlier entry: installed UE 5.8.2 `Engine/Source/Runtime/CoreUObject/Public/UObject/WeakObjectPtr.h:161-204` makes ordinary equality return true for any two invalid weak pointers, while `:371-379` hashes index/serial; `Runtime/Core/Public/UObject/WeakObjectPtrTemplates.h:398-434` supplies explicit `TWeakObjectPtrSetKeyFuncs`/`TWeakObjectPtrMapKeyFuncs` matching `HasSameIndexAndSerialNumber`; `CoreUObject/Public/UObject/ObjectKey.h:17-60` defines immutable `FObjectKey`. Guidance now rejects retaining stale weak keys with default key funcs and recommends explicit engine funcs or `TObjectKey` as appropriate. | Parent real probe used explicit `TWeakObjectPtrMapKeyFuncs`: UBT succeeded and fresh headless editor reported 1 success, 0 failures, 0 warnings; it confirmed stale ordinary equality collapses objects while explicit engine key funcs preserve distinct identity. No input assets were required. No GC stress, serialization, or cleanup benchmark. Node `v22.23.2` compatibility, scoped `skills-ref@0.1.5`, citations (4 Markdown files, 11 citations), and scoped `git diff --check` passed. |
| MEMORY-GC-02 | **Applied** | `skills/core/memory-and-gc/SKILL.md`; `skills/core/memory-and-gc/references/smart-pointers.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Core/Public/Templates/SharedPointerFwd.h:22-27` declares `TSharedPtr`, `TSharedRef`, `TWeakPtr`, and `TSharedFromThis` with default `ESPMode::ThreadSafe`; the explicit `NotThreadSafe` alternative is documented in the same source. | No multithreaded ownership, contention, or performance benchmark. The Windows Editor out-of-memory section remains partially verified as host/configuration guidance; no system settings were changed. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and core citation validation passed (4 Markdown files, 11 citations). |

## Batch-one correction reconciliations

### core/animation-system

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| ANIM-01 | **Corrected** | `skills/core/animation-system/SKILL.md` | Corrected the sample's cached bool to `bCachedIsFalling` and added braces around the movement-component conditional; this is a local C++ style/correctness correction with no UE API claim change. Qualified the existing `AnimInstance.h` citation with `Engine/Source/` so the Node22 citation checker recognizes it. | No fixture or animation runtime test; `check-hermes-compatibility.mjs`, `skills-ref@0.1.5`, and per-skill citations passed under Node `v22.23.2` (5 Markdown files, 1 recognized citation). |

### core/debugging-techniques

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| DEBUG-02 | **Corrected-to-report** | `skills/core/debugging-techniques/SKILL.md`; `skills/core/debugging-techniques/references/draw-debug-and-console.md` | Installed UE 5.8.2 `DrawDebugHelpers.cpp:125-145` selects transient World/Foreground batchers for nonpersistent `LifeTime<=0` and only uses `DefaultLifeTime` as the stored entry lifetime; `GameViewportClient.cpp:2105-2107` and `EditorViewportClient.cpp:4991-4996` flush World/Foreground batchers after drawing; `World.cpp:3463-3475` implements `FlushLineBatchers`. Replaced the false visible-1-second implication with transient per-draw flush versus positive-duration/persistent-batcher guidance. | No PIE/editor visual timing run; the correction is source/static and explicitly makes no 1-second visual-lifetime promise. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and core citation validation passed (4 Markdown files, 10 citations). |

### core/editor-scripting-and-python

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| EDITOR-PYTHON-01 | **Rejected** | Reverted only the task-owned `CallInEditor` syntax hunks in `skills/core/editor-scripting-and-python/SKILL.md` and `skills/core/editor-scripting-and-python/references/editor-utility-widgets.md`; preserved pre-existing gotchas. | Parent's real UE 5.8.2 UHT + headless editor probe built both `UFUNCTION(CallInEditor)` and `UFUNCTION(meta=(CallInEditor="true"))`; both had `GetBoolMetaData("CallInEditor")==true` and `ParmsSize==0`. Installed `UhtDefaultSpecifiers.cs:129-133` maps the direct specifier to the same metadata, and `PropertyCustomizationHelpers.cpp:1375` consumes that metadata plus zero parameters. The optional direct spelling is not a correctness fix. | Parent-owned fixture/editor run; no fixture edit here. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and core citation validation passed (4 Markdown files, 31 citations). |

### core/automation-and-testing

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| TEST-01 / TEST-02 | **Citation-corrected** | `skills/core/automation-and-testing/SKILL.md` | Qualified the existing P1-relevant `AutomationTest.h` source citation from `Runtime/Core/Public/...` to `Engine/Source/Runtime/Core/Public/...`; no testing guidance or source claim changed. This addresses the baseline citation parser's NO COVERAGE result for the bare source-relative path. | No additional UE test run; the existing parent-owned fixture/build evidence remains unchanged. Node `v22.23.2` compatibility, `skills-ref@0.1.5`, and per-skill citation validation passed (5 Markdown files, 1 recognized citation). |

### core/meshes-static-and-skeletal

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| MESH-01 | **Corrected-to-report** | `skills/core/meshes-static-and-skeletal/SKILL.md`; `skills/core/meshes-static-and-skeletal/references/static-meshes.md` | Follow-up correction: installed UE 5.8.2 `Engine/Source/Runtime/Engine/Private/Components/SceneComponent.cpp:2973-2990` returns the component transform for world/other fallback spaces but `FTransform::Identity` for component/parent-bone space; `StaticMeshComponent.cpp:1386-1420` falls back to that base behavior when a named socket is missing. The owner sentence now qualifies world/actor versus component/parent-bone space. | Parent’s UE 5.8.2 probe tested a transient unregistered static mesh: missing-socket world space returned the non-identity component transform and component space returned Identity. No render/assets/network test. Parent reran Node22/version, Hermes, scoped skills-ref, scoped citations, and diff checks; no attachment PIE, skeletal-bone, or packaged runtime test. |
| MESH-02 | **Applied** | `skills/core/meshes-static-and-skeletal/SKILL.md`; `skills/core/meshes-static-and-skeletal/references/static-meshes.md`; `skills/core/meshes-static-and-skeletal/references/skeletal-meshes.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/Engine/StaticMesh.h:650,743-746,855-882` places static Nanite settings/accessors inside `WITH_EDITORONLY_DATA`, and `SetNaniteSettings` only assigns while `NotifyNaniteSettingsChanged` calls `PostEditChangeProperty`; `Engine/SkeletalMesh.h:954-984` gives the same skeletal editor-only boundary and notification hook. | No editor asset mutation, package save, derived-data rebuild, cook, or runtime asset-setting test. The minimal examples now identify editor/asset-build scope and call the verified notification hook; no gameplay/project mutation was performed. |
| MESH-03 | **Applied** | `skills/core/meshes-static-and-skeletal/SKILL.md`; `skills/core/meshes-static-and-skeletal/references/skeletal-meshes.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Public/SkinnedMeshComponentHelper.h:197-205` gates Nanite skinning on disallow/force-disable flags, the global switch, platform support, and valid resources; `Engine/Private/Components/SkinnedMeshComponent.cpp:620-634` chooses the Nanite or traditional proxy; `Engine/Private/SkeletalRenderNanite.cpp:449-475` accepts morph inputs but initializes dynamic data without them. | No skeletal asset compile, morph/cloth render, material audit, platform/RHI, VSM, draw/pass count, or cooked runtime test. Guidance deliberately makes no blanket production, fallback, or one-draw promise. |
| MESH-04 | **Applied** | `skills/core/meshes-static-and-skeletal/references/instanced-meshes.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/Components/InstancedStaticMeshComponent.h:287-315` labels the ID interface preliminary, excludes HISM, warns about editor-edited ISMs, and declares `IsValidId`; the reference now carries those restrictions and removes the blanket stable-ID claim. | No ISM/HISM mutation, editor-ID invalidation, stress, or runtime retained-ID test. The full reference was read and sibling stable-ID wording in its version note was corrected; no unrelated instance guidance changed. |
| MESH-05 | **Corrected-to-report** | `skills/core/meshes-static-and-skeletal/references/instanced-meshes.md` | Follow-up correction: installed UE 5.8.2 `Engine/Source/Runtime/Engine/Private/InstancedStaticMesh.cpp:3987-3999` clears and zeroes `PerInstanceSMCustomData` only when the requested count differs from `NumCustomDataFloats`. The example now says “when the count changes.” | Parent’s UE 5.8.2 probe confirmed changing 2 → 4 resizes and resets values, while setting 2 → 2 preserves an existing value of 7. No material compile, render, upload, or broader instance mutation test. Parent reran Node22/version, Hermes, scoped skills-ref, scoped citations, and diff checks. |

### core/module-and-build-system

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| MODULE-01 | **Applied** | `skills/core/module-and-build-system/SKILL.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Projects/Public/ModuleDescriptor.h:101-107` marks `Developer` deprecated/ambiguous and recommends `UncookedOnly` or `DeveloperTool`; `ModuleDescriptor.cpp:600-615` filters `Developer` to editor/program and `DeveloperTool` by `bBuildDeveloperTools`; UBT `Configuration/Descriptors/ModuleDescriptor.cs:644-648` emits the deprecation warning. | No editor/program/game/server target matrix was built; source/static host filtering only. Read all four skill files and searched sibling module tables/examples; the combined row in `SKILL.md` was the only stale `Developer`/`DeveloperTool` rule. |
| MODULE-02 | **Applied** | `skills/core/module-and-build-system/SKILL.md`; `skills/core/module-and-build-system/references/build-cs-reference.md` | Installed UE 5.8.2 `UnrealBuildTool/Configuration/Rules/ModuleRules.cs:816-845` selects PCH behavior and forces `NoPCHs` for C++ modules; `:1133-1137` defines `IWYUSupport` and says `bEnforceIWYU` must be true; `Rules/ModuleRules.Obsolete.cs:24-35` marks module `bEnforceIWYU` obsolete; `TargetRules.cs:1779-1782` defines the active target-level gate; `UEBuildModuleCPP.cs:2433-2437` requires both. | No custom Build.cs/Target.cs UBT compile, IWYU pass, or C++20-module build. Guidance now separates PCH selection from module support and target enforcement without changing the valid modern PCH example. Node `v22.23.2` Hermes compatibility, `skills-ref@0.1.5`, citations (4 files, 11 citations), and `git diff --check` passed. |

### core/mover-movement-system

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| MOVER-01 | **Applied** | `skills/core/mover-movement-system/SKILL.md` | Installed UE 5.8.2 `Engine/Plugins/Experimental/Mover/Source/Mover/Public/MoverComponent.h:223-242` documents the input-producer flag; `Private/MoverComponent.cpp:292-312` unconditionally enumerates owner components implementing the interface; `:340-353` invokes every valid `InputProducers` entry. The guidance now reports the header/source mismatch and does not promise the flag suppresses component producers. | No runtime actor with the flag set false, input-order, PIE, network, or Epic-confirmation test. Source-only qualification and explicit-producer containment guidance. |
| MOVER-02 | **Corrected** | `skills/core/mover-movement-system/SKILL.md`; `skills/core/mover-movement-system/references/layered-moves-and-instant-effects.md` | Installed UE 5.8.2 `Mover/Public/MoverComponent.h:316-334` calls general automatic layered-move networking pending and says only Chaos Mover networks them by default; `ChaosMover/Public/ChaosMover/Backends/ChaosMoverBackend.h:135-149` exposes networked/local injection and action paths. The prose still separates rollback from backend transport; the reference now says “Queue moves with `UMoverComponent::QueueLayeredMove` rather than applying them immediately. Moves are cloned on queueing, so configure them fully before queueing.” | No live Network Prediction/ChaosMover transport, rollback, correction, or dedicated-server test; this follow-up changed grammar only and did not change the transport guidance. |
| MOVER-03 | **Applied** | `skills/core/mover-movement-system/SKILL.md` | Installed UE 5.8.2 `Mover/Public/MoverComponent.h:185-186` says `ProduceInput` runs on the owner’s instance, autonomous or authority; `MoverSimulationTypes.h:514-525` defines the producer interface; `ChaosMover/Private/Backends/ChaosMoverBackend.cpp:645-658` has separate input/server-input paths. The “locally-controlled only” statement was replaced with owning-instance/prediction-safe guidance. | No server AI, authority-owned actor, simulated-proxy, or rollback runtime test. Determinism and no-side-effect guidance is source-based. |
| MOVER-04 | **Applied** | `skills/core/mover-movement-system/references/layered-moves-and-instant-effects.md` | Installed UE 5.8.2 `Mover/Public/MoverComponent.h:255-285` supplies the activation template default but accepts an explicit class; `Mover/Private/MoverComponent.cpp:2665-2683` searches registered moves using the supplied `MoveLogicClass`. The sample now passes `UMyDashLogic::StaticClass()`. | No registered logic activation or Blueprint/C++ compile test; the correction is a direct template/class-lookup check. |
| MOVER-05 | **Applied** | `skills/core/mover-movement-system/references/layered-moves-and-instant-effects.md` | Installed UE 5.8.2 `Mover/Public/LayeredMove.h:74-176` defines the virtual `NetSerialize` contract; `Mover/Private/LayeredMove.cpp:551-616` dispatches each derived move’s serializer after reconstructing its script struct. The homing sample now captures `TargetLocation`, reads only that deterministic value, and serializes it alongside `Speed`. | No live network serialization, actor-target identifier, async, or rollback test. Dynamic actor targeting remains intentionally deferred because this sample has no network-resolvable identity contract. |
| MOVER-06 | **Applied** | `skills/core/mover-movement-system/references/layered-moves-and-instant-effects.md` | Installed UE 5.8.2 `Mover/Public/InstantMovementEffect.h:59-85` declares virtual `NetSerialize`; `Mover/Private/InstantMovementEffect.cpp:14-17` leaves the base serializer empty; `ChaosMover/Private/ChaosMoverSimulationTypes.cpp:231-278` dispatches the concrete effect serializer for networked effects. The sample now marks `ModeName` as reflected state and serializes it. | No ChaosMover network session or received-effect test; no fixture/project mutation. Node `v22.23.2` Hermes compatibility, `skills-ref@0.1.5`, citations (5 files, 18 citations), and `git diff --check` passed. |

### core/nanite-and-rendering

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| NANITE-RENDER-01 | **Applied** | `skills/core/nanite-and-rendering/SKILL.md`; `skills/core/nanite-and-rendering/references/nanite.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Public/Rendering/NaniteResourcesHelper.h:154-185,253-282,323-347` audits material blend/shading support and can reject Nanite proxy creation; `Runtime/Renderer/Private/Nanite/NaniteShading.cpp:1102-1112` rejects unsupported materials before the base pass and retries through the caller’s fallback path. A source search found no `r.Nanite.ShowMaskedMaterialWarnings` or `r.Nanite.Validate` declaration; the guidance now uses Nanite visualization, Output Log, and Primitive Debugger checks. | No translucent asset, material audit, fallback render, RHI/platform, or cooked-build run. The pre-existing Native foliage wind section in `SKILL.md` was preserved unchanged. |
| NANITE-RENDER-02 | **Applied** | `skills/core/nanite-and-rendering/SKILL.md`; `skills/core/nanite-and-rendering/references/nanite.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/Engine/RendererSettings.h:1549-1553` labels `r.Nanite.Foliage` “Nanite Foliage (Experimental),” describes experimental features, and requires editor restart. The guidance now scopes foliage to target validation and retains a conventional fallback, incorporating the audited UE 5.8.2 hotfix gate. | No foliage asset, platform matrix, restart, WPO amplitude, fallback, or 5.8.2 cooked/render test. Hotfix evidence is report-level documentation, not a local runtime test. |
| NANITE-RENDER-04 | **Applied** | `skills/core/nanite-and-rendering/references/virtual-textures-and-shadows.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/Engine/RendererSettings.h:368-370` deprecates `bMobileVirtualTextures`; `Engine/Source/Runtime/Engine/Private/Texture.cpp:66-78` declares `r.VirtualTextures` and emits a deprecation ensure for `r.Mobile.VirtualTextures`, directing callers to per-platform engine ini overrides. | No mobile platform cook, RVT/material support, or device run. The old mobile-specific setting/CVar was removed from the recipe, not revalidated on hardware. |
| NANITE-RENDER-05 | **Applied** | `skills/core/nanite-and-rendering/references/virtual-textures-and-shadows.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/Engine/RendererSettings.h:665-668` states that Forward Shading automatically uses ordinary Shadow Maps because Virtual Shadow Maps are unsupported. The VR/forward snippet and version note now say not to rely on `r.Shadow.Virtual.Enable=1` for that project path. | No forward/VR project settings load, RHI, VSM page, Nanite fallback, or runtime render test. The correction is limited to the documented project-setting contract. |

### core/navigating-engine-source

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| NAV-SOURCE-01 | **Applied** | `skills/core/navigating-engine-source/SKILL.md`; `skills/core/navigating-engine-source/references/module-map.md`; `skills/core/navigating-engine-source/references/source-conventions.md` | Installed `C:/Program Files/Epic Games/UE_5.8/Engine/Build/Build.version:1-10` reports UE 5.8.2, changelist 56702186, compatible changelist 55116800, promoted branch `++UE5+Release-5.8`. The three stale 5.8.1/CL 56057345 claims now use that pinned build identity. | No second engine installation/project association, source checkout comparison, or compile was needed; version provenance was read from the installed file. |
| NAV-SOURCE-02 | **Applied** | `skills/core/navigating-engine-source/SKILL.md`; `skills/core/navigating-engine-source/references/source-conventions.md` | Installed UE 5.8.2 UHT `Source/Programs/Shared/EpicGames.UHT/Types/UhtClass.cs:587-622,1797-1810` computes class/interface/deprecated/Verse prefixes and validates reflected class names; `UhtScriptStruct.cs:898-904` selects `T` or `F` for reflected structs via configuration. Guidance now scopes prefix errors to reflected declarations and preserves ordinary-C++ navigation heuristics. | No UHT run with invalid class/struct names, generated-code test, or custom `T`-prefix configuration. Node `v22.23.2` Hermes compatibility, `skills-ref@0.1.5`, citations (4 files, 3 citations), and `git diff --check` passed. |

### core/networking-and-replication

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| NET-REPL-01 | **Applied** | `skills/core/networking-and-replication/SKILL.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/GameFramework/Actor.h:902-905` marks public `NetUpdateFrequency` access `UE_DEPRECATED(5.5)` and directs callers to `SetNetUpdateFrequency`/`GetNetUpdateFrequency`; the constructor example now uses the setter. | No custom actor compile or replication-rate runtime test. |
| NET-REPL-02 | **Applied** | `skills/core/networking-and-replication/references/fast-arrays.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Net/Core/Classes/Net/Serialization/FastArraySerializer.h:180-224` distinguishes generic array delta serialization (iterating elements/property deltas) from Fast Array ID/key tracking and explicitly notes indices are not synchronized. The reference no longer promises universal whole-array transfer. | No bandwidth/CPU profile or representative element-type comparison; actual wire cost remains workload-dependent. |
| NET-REPL-03 | **Applied** | `skills/core/networking-and-replication/references/fast-arrays.md` | Installed UE 5.8.2 `FastArraySerializer.h:517-537` defines the serializer-level `FPostReplicatedReceiveParameters` detection contract; `:699-705` calls `ArraySerializer.PostReplicatedReceive` after a receiving delta. The example and callback table now put this hook on `FInventoryArray`, not the item. | No custom Fast Array compile or live receive callback test; the per-item callback timing remains source-based. |
| NET-REPL-04 | **Applied** | `skills/core/networking-and-replication/references/replication-conditions-and-push-model.md` | Installed UE 5.8.2 UBT `Engine/Source/Programs/UnrealBuildTool/Configuration/Rules/TargetRules.cs:1516-1527` exposes target-level `bWithPushModel`; `Runtime/Net/Core/Public/Net/Core/PushModel/PushModelMacros.h:5-7` consumes the compile-time macro. The guidance separates target rules/project generation from runtime `net.IsPushModelEnabled`. | No target regeneration, generated macro inspection, or packaged target run. |
| NET-REPL-05 | **Applied** | `skills/core/networking-and-replication/SKILL.md`; `skills/core/networking-and-replication/references/rpcs.md` | Installed UE 5.8.2 UHT `Programs/Shared/EpicGames.UHT/Specifiers/UhtFunctionSpecifiers.cs:316-326` sets `EFunctionFlags::NetValidate`; `Types/UhtFunction.cs:899-924` requires the validation implementation only when that flag is present; `Exporters/CodeGen/UhtHeaderCodeGeneratorCppFile.cs:3505-3513` conditionally emits the `_Validate` call. The docs now keep authoritative checks in `_Implementation`. | No custom UHT/generated thunk, malicious RPC, disconnect, or server authorization test. |
| NET-REPL-06 | **Applied** | `skills/core/networking-and-replication/SKILL.md`; `skills/core/networking-and-replication/references/replication-conditions-and-push-model.md` | Installed UE 5.8.2 `Runtime/Net/Iris/Private/Iris/IrisConfig.cpp:15-30` defaults `net.Iris.UseIrisReplication` to `0`; `Engine/Classes/GameFramework/Actor.h:3476-3499` exposes `OnReplicationStartedForIris`/`OnStopReplicationForIris` and deprecates `BeginReplication`/`EndReplication`. The prose now distinguishes Epic’s licensee release-note status from public opt-in/default behavior and corrects the lifecycle hooks. | No Iris server/client session, protocol-compatibility, enabled-target, or lifecycle callback test; release-note maturity remains distribution-scoped. |

### core/niagara-vfx

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| NIAGARA-01 | **Applied** | `skills/core/niagara-vfx/SKILL.md`; `references/data-interfaces-and-performance.md` | Installed UE 5.8.2 `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraDataInterfaceCollisionQuery.h:24-64` exposes CPU query functions, shader functions, and `CanExecuteOnTarget` for both targets; `NiagaraDataInterfaceAsyncGpuTrace.h:13-49` restricts Async Gpu Trace to `GPUComputeSim` and exposes ray-tracing-scene requirements. `NiagaraDataInterfaceCollisionQuery.cpp:549-664` performs CPU collision batches, while the GPU interface contract remains scene-data based. The docs now separate CPU world-geometry queries, GPU scene paths, and Experimental HWRT. | Source/declaration and versioned-doc verification only; no Niagara graph, CPU/GPU collision, DXR, platform, or hardware runtime test. |
| NIAGARA-02 | **Applied** | `skills/core/niagara-vfx/references/data-interfaces-and-performance.md` | Installed UE 5.8.2 `NiagaraDataChannelFunctionLibrary.h:20-50` declares `UNiagaraDataChannelLibrary` and marks the SearchParameters writer legacy; `:137-163` declares `WriteToNiagaraDataChannel_WithContext` with `FNDCAccessContextInst&`; `NiagaraDataChannelAccessContext.h:112-169,215-276` declares `FNDCAccessContext`, `TNDCAccessContextType`, and `FNDCAccessContextInst`. The example now uses the current access-context spelling and labels the old overload legacy. | Parent UE 5.8.2 editor probe compiled the writer signature and exercised access-context `Init`/`GetChecked` at runtime. No Niagara Data Channel asset/read-write session, module-dependency, Blueprint-exposure, world/tick, network, or package proof. |
| NIAGARA-03 | **Applied** | `skills/core/niagara-vfx/SKILL.md`; `references/system-and-emitter-model.md` | Installed UE 5.8.2 `NiagaraCommon.h:1102-1132` lists System/Emitter/Particle and event/simulation-stage script usages but no Render module-script usage; `NiagaraEmitterHandle.h:101-102` exposes enabled-renderer iteration. The hierarchy/reference now treats renderers as output items rather than a writable module stack group. | Source/documentation model verification only; no Niagara Editor stack, renderer binding, namespace validation, or compiled asset test. |
| NIAGARA-04 | **Applied** | `skills/core/niagara-vfx/SKILL.md`; `references/system-and-emitter-model.md` | Installed UE 5.8.2 `NiagaraEmitterHandle.h:15-20,81-95` declares Standard/Stateless modes and mode-specific accessors; `Internal/Stateless/NiagaraStatelessEmitter.h:25-34` declares `UNiagaraStatelessEmitter`; the pinned 5.8.2 hotfix cache records the null `Handle.GetEmitterData()` fix for Stateless original compilation. The guidance now branches on mode and does not require non-null standard emitter data. | No Stateless emitter asset compile, original compilation, editor load, or runtime simulation test. |
| NIAGARA-05 | **Applied** | `skills/core/niagara-vfx/references/spawning-and-parameters.md` | Installed UE 5.8.2 `NiagaraSystemInstanceController.h:11-25,107-112` defines `NIAGARA_SYSTEM_INSTANCE_CONTROLLER_ASYNC` as pass-through by default and deferred when enabled; `NiagaraComponent.cpp:2695-2698` routes setters through `SetVariable_Deferred`. The reference now describes a subsequent applicable simulation step rather than a universal one-frame delay. | No project tick-group, activation-order, controller-configuration, PIE, or same-frame parameter timing test. |
| NIAGARA-06 | **Applied** | `skills/core/niagara-vfx/references/spawning-and-parameters.md` | Installed UE 5.8.2 `NiagaraComponentPool.h:65-83` declares the pool as a `UObject`; `NiagaraWorldManager.h:199-200` exposes `GetComponentPool`; `NiagaraComponent.cpp:1972-1979` calls `SetUserParametersToDefaultValues()` during `OnPooledReuse`, and `:816-845`/`:1866-1878` show pool reclaim paths. The reference now separates automatic reuse reset from `ManualRelease` ownership. | No pool allocation/reuse performance, manual-release lifecycle, parameter-bleed runtime, or pooled component test. |
| NIAGARA-08 | **Applied** | `skills/core/niagara-vfx/SKILL.md`; `references/data-interfaces-and-performance.md` | Installed UE 5.8.2 `NiagaraGpuComputeDispatchInterface.h:29-35` exposes GPU dispatch work and `NiagaraDataInterface.h:724-734` exposes overlap behavior; `NiagaraSortingGPU.cpp:13-26` shows configurable sort/cull thresholds rather than an engine-wide particle limit. The guidance replaces portable `~10k`/millions/near-zero thresholds with measured module, renderer, memory, platform, and frame-budget criteria. | No target project profile, Niagara Debugger capture, GPU benchmark, renderer comparison, or CPU/GPU migration test. Node `v22.23.2` Hermes compatibility, cached `skills-ref@0.1.5` validation, scoped citations (4 Markdown files, 9 citations), and scoped `git diff --check` passed; the repository-prescribed `node_modules/npm/bin/npx-cli.js` path was absent, so the cached skills-ref CLI was invoked directly with the explicit Node 22 executable. |

### core/packaging-and-deployment

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| PACK-01 | **Applied** | `skills/core/packaging-and-deployment/references/buildcookrun-and-uat.md`; `references/cook-and-build-configs.md` | Installed UE 5.8.2 `AutomationTool/AutomationUtils/ProjectParams.cs:845-858,922-934,1644-1654` parses `SkipCookingEditorContent`, `Manifests`, and `CreateChunkInstall`; `:3359-3372` requires stage/manifests plus chunk-install directory/version. `ProjectPackagingSettings.h:461-477` declares `bTreatWarningsAsErrorsOnCook`, and `CookOnTheFlyServer.cpp:6630-6634` applies it to `GWarn->TreatWarningsAsErrors`; no checked `ProjectParams` switch establishes `-warningsaserrors` or `-generatechunks`. | Source/static UAT verification only; no RunUAT command, custom wrapper, cook, manifest, or chunk-install run. |
| PACK-02 | **Applied** | `skills/core/packaging-and-deployment/SKILL.md`; `references/cook-and-build-configs.md` | UE 5.8 release notes cache `:1141-1151` labels Incremental Cooking Beta, describes Zenserver as the cooked-output default for the new workflow, and preserves the existing-project-disabled exception. Installed UE 5.8.2 `ProjectPackagingSettings.h:248-254,667-670` exposes `bUseIoStore`, `bUseZenStore`, and the effective `bUseZenStore && bUseIoStore` gate; `ProjectPackagingSettings.cpp:41-47` warns on Zen without IoStore. | No clean/iterative/incremental cook comparison, Zen lifecycle, DDC, determinism, or project/platform configuration test. |
| PACK-03 | **Applied** | `skills/core/packaging-and-deployment/SKILL.md`; `references/platform-and-dlc.md` | Installed UE 5.8.2 `OnDemandCore/Public/IO/IoStoreOnDemand.h:45-82,646-782` exposes request statuses, `IOnDemandIoStore`, mount/install, and `TryGetOnDemandIoStore`; `ProjectParams.cs:1118-1129,2535-2542` makes `-applyiostoreondemand` opt-in, adds `-CompileIoStoreOnDemand`, forces manifests, and defaults `IoStoreOnDemandSettings` to a `RuleMode::None` object (`IoStoreOnDemandUtils.cs:9-24,39-46,82-96`). No installed `*IoStoreOnDemand*.uplugin` descriptor was used as a premise. | No On-Demand host, authorization, mount/install, patch, platform, CDN, or packaged shipping test. |
| PACK-04 | **Applied** | `skills/core/packaging-and-deployment/SKILL.md`; `references/pak-iostore-and-chunking.md`; `references/platform-and-dlc.md` | Installed UE 5.8.2 `ProjectPackagingSettings.h:260-304` describes chunk generation, catch-all/dependency options, and size splitting; `ProjectParams.cs:1644-1654` exposes manifest/chunk-install controls. The docs now separate packaging chunk metadata from platform/launcher/DLC delivery and mount policy, including the legacy `FPakPlatformFile::Mount()` limitation. | No project cook/stage/package, platform launcher, HTTP Chunk Installer, ChunkDownloader, DLC, patch manifest, IoStore mount, or runtime delivery test. Node `v22.23.2` Hermes compatibility, cached `skills-ref@0.1.5` validation, scoped citations (5 Markdown files, 14 citations), and scoped `git diff --check` passed; the repository-prescribed `node_modules/npm/bin/npx-cli.js` path was absent, so the cached skills-ref CLI was invoked directly with the explicit Node 22 executable. |

### core/physics-and-chaos

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| PHYS-01 | **Applied** | `skills/core/physics-and-chaos/SKILL.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Private/Components/SceneComponent.cpp:3070-3095` combines component responses with the minimum response; `PrimitiveComponent.cpp:223-231` requires both components’ overlap-event flags and a resolved `ECR_Overlap`. The owner rule and gotcha now state the overlap/block combination without the prior contradiction. | Source/static verification only; no runtime overlap, trigger, collision-profile, or event-delegate test. |
| PHYS-02 | **Applied** | `skills/core/physics-and-chaos/SKILL.md`; `references/physics-simulation-and-constraints.md` | Installed UE 5.8.2 `PhysicsSettings.h:315-345` declares `bSubstepping`, `bSubsteppingAsync`, `bTickPhysicsAsync`, `AsyncFixedTimeStepSize`, `MaxSubstepDeltaTime`, and `MaxSubsteps`, with async/substep settings marked experimental. `ActorComponent.h:402-407,978-985` documents game-thread callback execution and full PT/GT synchronization; `ActorComponent.cpp:1842-1863` shows registration. The obsolete `AsyncFixed`/Use Async Scene contract was removed. | No async physics/substep runtime, determinism, latency, thread-contention, or project-settings test. |
| PHYS-03 | **Applied** | `skills/core/physics-and-chaos/SKILL.md`; `references/physics-simulation-and-constraints.md` | Installed UE 5.8.2 `PhysicalAnimationComponent.h:66-100` declares `UPhysicalAnimationComponent` with the `Experimental` UCLASS specifier and the profile APIs used by the example. The partial-ragdoll guidance now retains the path but requires experimental-status and production-fallback validation. | No ragdoll, animation handoff, network, save/load, teardown, platform, or fallback runtime test. |
| PHYS-04 | **Applied** | `skills/core/physics-and-chaos/SKILL.md`; `references/physics-simulation-and-constraints.md` | Installed UE 5.8.2 `PrimitiveComponent.h:448-453` declares `UPrimitiveComponent::bReturnMaterialOnMove` for component sweeps; `HitResult.h:118-123` makes `PhysMaterial` a weak pointer; `CollisionQueryParams.h:42-60` declares explicit-query `bReturnPhysicalMaterial`. The owner/reference now separate movement-hit and query contracts and check `IsValid()` before reading `SurfaceType`. The pre-existing decorative collision section was preserved; `StaticMeshComponent.cpp:2618-2622` verifies named profile application disables `bUseDefaultCollision`. | No trace/material, movement-hit, saved-map reopen, simple/complex query, or decorative collision runtime test. Node `v22.23.2` Hermes compatibility, cached `skills-ref@0.1.5` validation, scoped citations (4 Markdown files, 13 citations), and scoped `git diff --check` passed; the repository-prescribed `node_modules/npm/bin/npx-cli.js` path was absent, so the cached skills-ref CLI was invoked directly with the explicit Node 22 executable. |

### core/plugins-and-modules

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| PLUG-01 | **Applied** | `skills/core/plugins-and-modules/SKILL.md`; `references/plugin-dependencies-and-packaging.md` | Installed UE 5.8.2 `BuildPluginCommand.Automation.cs:419-488` applies `FilterPluginFiles`, whose default rules omit arbitrary `Config/*.ini` and load `Config/FilterPlugin*.ini`; `Scripts/CopyBuildToStagingDirectory.Automation.cs:1923-1949` enumerates staged `.uplugin` files and calls `SC.StageFiles(StagedFileType.UFS, PluginConfigDirectory, "*.ini", StageFilesSearch.AllDirectories)` for each existing plugin `Config` directory. The audit’s named `GetPluginConfigFiles` helper was not present in this installed source; the conclusion uses the actual generic staging path. | No BuildPlugin filter file, cooked target, plugin config, or packaged output run. |
| PLUG-02 | **Applied** | `skills/core/plugins-and-modules/SKILL.md`; `references/plugin-dependencies-and-packaging.md`; `references/uplugin-descriptor.md` | Installed UE 5.8.2 `PluginReferenceDescriptor.cpp:137-178` reads `Activate`, serialized `Version` into `RequestedVersion`, and rejects a version when `Enabled` is false; `PluginReferenceDescriptor.h:34-41,67-74` declares `bActivate` and `RequestedVersion`. The docs now distinguish serialized keys and GameFeature activation. | Parent UE probe exercised `FPluginReferenceDescriptor` parsing of `Version`/`Activate` and rejected a version with `Enabled=false`; no GameFeaturePlugin activation/deactivation, parent lifecycle, cooked/plugin load, or package test. |
| PLUG-03 | **Applied** | `skills/core/plugins-and-modules/references/plugin-dependencies-and-packaging.md` | Installed UE 5.8.2 `PluginManager.cpp:2688-2714` parses `EngineVersion`, compares it with `FEngineVersion::CompatibleWith()`, and can prompt to load an incompatible plugin; `BuildPluginCommand.Automation.cs:419-445` writes `Major.Minor.0` unless unversioned; `ModuleDescriptor.cpp:843-860` and `ModuleManager.cpp:372-386` provide separate module path/up-to-date checks. The prose now treats `EngineVersion` as a compatibility gate, not ABI proof. | No cross-engine binary, load prompt, hot-reload, OS-loader, or ABI test. |
| PLUG-04 | **Applied** | `skills/core/plugins-and-modules/references/plugin-dependencies-and-packaging.md` | Installed UE 5.8.2 `IPluginManager.h:545-589` requires `FText*` for `UnmountExplicitlyLoadedPlugin`, documents that compiled-module plugins cannot be unmounted, and separates localization-data mount/unmount calls. The reference now supplies the reason argument and limitation. | No explicit-load mount/unmount, localization, compiled-module unload, or runtime session. Node `v22.23.2` Hermes compatibility, cached `skills-ref@0.1.5` validation, scoped citations (4 Markdown files, 6 citations), and scoped `git diff --check` passed; the repository-prescribed `node_modules/npm/bin/npx-cli.js` path was absent, so the cached skills-ref CLI was invoked directly with the explicit Node 22 executable. |

### core/profiling-and-optimization

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| PROF-01 | **Applied** | `skills/core/profiling-and-optimization/SKILL.md` | Versioned Epic UE 5.8 Stat Commands documentation describes `GPU` as displaying GPU statistics for the frame; installed UE 5.8.2 `TraceAuxiliary.h:142-152` separately starts trace channels. The table now distinguishes ordinary `stat gpu` triage from the Insights GPU track. | No in-game `stat gpu`, RHI/platform, GPU Visualizer, or Insights GPU capture. |
| PROF-02 | **Applied** | `skills/core/profiling-and-optimization/references/stat-commands.md`; `references/instrumenting-cpp.md` | Installed UE 5.8.2 `Stats.h:39-72,120-132,171-176,206-216` defines the type-only `DECLARE_STATS_GROUP`, `DECLARE_CYCLE_STAT`, and `DECLARE_CYCLE_STAT_EXTERN` patterns; a literal source search found no `DECLARE_STATS_GROUP_EXTERN` or `DECLARE_STAT_GROUP_EXTERN`. The cross-file example now uses the supported group/stat split. | Parent UE probe compiled and linked the stats group/extern/definition pattern in a single translation unit; no cross-translation-unit one-definition or production module test. |
| PROF-03 | **Applied** | `skills/core/profiling-and-optimization/SKILL.md`; `references/instrumenting-cpp.md`; `references/memory-profiling.md` | Installed UE 5.8.2 `MemoryTrace.h:17-27` gates memory tracing on availability/non-Shipping policy; `LowLevelMemTrackerDefines.h:10-30` derives config support; `LowLevelMemTracker.h:16-31` derives `ENABLE_LOW_LEVEL_MEM_TRACKER` and deprecates all four named LLM defines; `TagTrace.h:29-34` gates memory-tag tracing; `CsvProfiler.h:44-53,93-120` and `:114-120` gate categories/timing on `CSV_PROFILER` and `CSV_PROFILER_MINIMAL`. The docs now make memalloc, memtag, LLM, and CSV promises target/config dependent. The pre-existing `references/windows-pie-baselines.md` and main-skill pointer were read and preserved unchanged. | No target build-define matrix, packaged Development/Test/Shipping trace, LLM capture, CSV capture, or Windows PIE integration run. Node `v22.23.2` Hermes compatibility, cached `skills-ref@0.1.5` validation, scoped citations (6 Markdown files, 8 citations), and scoped `git diff --check` passed; the repository-prescribed `node_modules/npm/bin/npx-cli.js` path was absent, so the cached skills-ref CLI was invoked directly with the explicit Node 22 executable. |

### core/project-structure

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| PROJ-01 | **Applied** | `skills/core/project-structure/references/uproject-and-modules.md` | Installed UE 5.8.2 `ProjectDescriptor.cpp:103-129` reads `FileVersion`, `Enterprise`, and `DisableEnginePluginsByDefault`; `:147-189` reads `AdditionalPluginDirectories` and editor-only `AdditionalRootDirectories`; `:269-318` writes the current serialized keys. The reference now distinguishes JSON names from C++ members and documents both directory lists. | Parent UE probe parsed `Enterprise` and `DisableEnginePluginsByDefault` through `FProjectDescriptor`; no `.uproject` write, launcher, editor, external-directory, or full project-load test. |
| PROJ-02 | **Applied** | `skills/core/project-structure/references/uproject-and-modules.md` | Installed UE 5.8.2 `ModuleDescriptor.cpp:196-252` reads current/deprecated target keys plus target-configuration, program, game-target, platform-architecture, and explicit-platform fields; `:283-287,352-459` writes current names. The table now removes unsupported `IncludelistTargets` and exposes the verified filters. | No multi-target, configuration, architecture, program, platform-extension, or UBT matrix build. |
| PROJ-03 | **Applied** | `skills/core/project-structure/SKILL.md`; `references/uproject-and-modules.md` | Installed UE 5.8.2 `ModuleManager.h:881-904` defines `FDefaultGameModuleImpl::IsGameModule()` as true, and `:1094-1136` defines the primary macro; `PluginReferenceDescriptor.cpp:137-178` reads recognized plugin-reference fields without a `Type` selector. The primary example now uses `FDefaultGameModuleImpl` and the plugin example no longer claims a project-classifying `Type` key. | No project module compile/load or `.uproject` plugin-discovery test. |
| PROJ-04 | **Applied** | `skills/core/project-structure/references/config-system.md`; `references/folder-layout-and-vcs.md` | Installed UE 5.8.2 `ConfigHierarchy.h:9-45` contains generated/custom/platform layers; `Obj.cpp:3678-3710,3958-3997` resolves `SaveConfig` through the class config filename and exposes explicit default/global-user/project-user paths; `Object.h:1280-1316` declares those APIs; `ConfigContext.cpp:1227-1234` shows a dynamic layer path. The references now qualify the abbreviated hierarchy, save destination, CVar section exceptions, and Saved/Config ownership. | No settings UI/save operation, platform-extension, generated-layer, user-config, or config precedence runtime test. Node `v22.23.2` Hermes compatibility, cached `skills-ref@0.1.5` validation, scoped citations (4 Markdown files, 17 citations), and scoped `git diff --check` passed; the repository-prescribed `node_modules/npm/bin/npx-cli.js` path was absent, so the cached skills-ref CLI was invoked directly with the explicit Node 22 executable. |

### core/save-and-load

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| SAVE-01 | **Applied** | `skills/core/save-and-load/references/savegame-objects-and-slots.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Private/GameplayStatics.cpp:2371-2387` constructs `FObjectAndNameAsStringProxyArchive`; `:2428-2436` routes slot saving through memory serialization. The parent UE probe round-tripped ordinary and SaveGame-marked properties and confirmed exclusion of a Transient property. | In-memory round trip only; no platform I/O, persisted slot, or old-save corpus test. |
| SAVE-02 | **Applied** | `skills/core/save-and-load/SKILL.md`; `references/versioning-and-migration.md` | Installed UE 5.8.2 `SaveGame.h:83-95,143-182` documents request acceptance, `WasLastSaveSuccessful()`, and `HandlePostSave(bool)`; `SaveGame.cpp:142-192,195-210` reports completion through `ProcessSaveComplete`; `GameplayStatics.h:1145-1155` exposes the generic async completion result. | Source/static verification only; no platform write, callback-ordering, or failure-injection test. |
| SAVE-03 | **Applied** | `skills/core/save-and-load/SKILL.md`; `references/versioning-and-migration.md` | `SaveGame.h:40-44,152-168` requires initialization after generic loading; `Engine/Private/GameFramework/SaveGame.cpp:111-129,300-314` supplies it in local-player helpers. `GameplayStatics.cpp:2457-2488,2536-2545` constructs/serializes without invoking `UObject::PostLoad` or `HandlePostLoad`. Parent probe confirmed the generic memory load does not invoke the local-player hook. Matching generic examples now explicitly call a migration method. | No local-player association, migration corpus, or platform I/O test. |
| SAVE-05 | **Applied** | `skills/core/save-and-load/SKILL.md`; `references/versioning-and-migration.md` | Narrowed the audit recommendation after parent review: binary `CoreUObject/Private/UObject/Class.cpp:1622,1688-1698` gates redirects on cooked-data/archive flags. `GameplayStatics.cpp:2484-2485` does not set `ArIsSaveGame`; parent runtime probe confirmed that flag is false in the stock loader. Guidance now preserves original serialized names unless redirects are verified in the actual target; adding `_Deprecated` is not a migration. | No cooked redirect or pre-rename save corpus test; editor success must not be treated as packaged compatibility. |
| SAVE-06 | **Applied** | `skills/core/save-and-load/SKILL.md`; `references/versioning-and-migration.md` | Installed UE 5.8.2 `GameplayStatics.cpp:2457-2488` returns null for empty/unresolved-class data but otherwise constructs, serializes, and returns the object; `SaveGameSystem.h:23-43` states platform existence-result codes are not universal. | Source/static verification only; no corrupt-file, checksum, or platform-specific recovery test. |

### core/sequencer-and-cinematics

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| SEQ-01 | **Applied** | `skills/core/sequencer-and-cinematics/references/movie-render-queue.md` | Installed UE 5.8.2 `MoviePipelineBasicConfig.cpp:173-179` initializes `{project_dir}/Saved/MovieRenders/`; `MoviePipelineVideoOutputBase.cpp:29-34,59-63` and `MoviePipelineCommandLineEncoder.cpp:97-107` consume the output directory as a filesystem path. | Source/static verification only; no MRQ render or output-permission test. |
| SEQ-02 | **Applied** | `skills/core/sequencer-and-cinematics/SKILL.md`; `references/movie-render-queue.md` | Installed UE 5.8.2 `MovieRenderPipeline.uplugin:56-79` declares runtime `MovieRenderPipelineCore`/`MovieRenderPipelineRenderPasses` and editor-only `MovieRenderPipelineEditor`; `MoviePipelineQueueEngineSubsystem.h:13-24` documents the runtime subsystem, while `MoviePipelineQueueSubsystem.h:13-16` is an editor subsystem. | Source/static verification only; no packaged MRQ cook, runtime render, codec/pass availability, or target-platform test. Node `v22.23.2`: scoped skills-ref, core citations (5 files, 13 citations), Hermes compatibility, and scoped `git diff --check` all passed. |

### core/subsystems

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| SUB-01 | **Applied** | `skills/core/subsystems/SKILL.md`; `references/lifecycle-and-access.md`; `references/subsystem-types.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Public/Streaming/StreamingWorldSubsystemInterface.h:8-23` declares `IStreamingWorldSubsystemInterface::OnUpdateStreamingState` and `OnFlushStreaming`; `WorldSubsystem.h:15-70` contains the current `UWorldSubsystem` API and no `UpdateStreamingState`. | Source/static verification only; no custom subsystem compile, streaming-world, or callback-order test. |
| SUB-02 | **Applied** | `skills/core/subsystems/references/choosing-a-subsystem.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Public/Subsystems/Subsystem.h:49-62` documents CDO-time `ShouldCreateSubsystem`; `KismetSystemLibrary.h:300-304` confirms `IsServer` requires a world context, so the example now uses the explicit dedicated-server predicate. | Source/static verification only; no dedicated/listen/client subsystem matrix or PIE run. Node `v22.23.2`: scoped skills-ref, core citations (4 files, 12 citations), Hermes compatibility, and scoped `git diff --check` all passed. |

### core/timers-and-async

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| TMR-01 | **Applied** | `skills/core/timers-and-async/SKILL.md`; `references/timer-manager.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Public/TimerManager.h:242-273` returns `FTimerHandle`; `:275-288` documents cancellation. Parent UE probe created a standalone timer manager, confirmed a live next-tick handle, cleared it, and confirmed invalidation/removal. | No world tick, callback scheduling, dilation, or teardown race test. |
| TMR-02 | **Applied** | `skills/core/timers-and-async/SKILL.md`; `references/timer-manager.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Private/TimerManager.cpp:1212-1237,1247-1309,1344-1349` computes overdue looping `CallCount`, honors `bMaxOncePerFrame`, and reschedules by `CallCount * Rate`; `Engine/Source/Runtime/Engine/Classes/GameFramework/Actor.h:792-798` limits `CustomTimeDilation` to actor tick delta. | Source/static verification only; no hitch, pause, world-dilation, or actor-custom-dilation runtime test. |
| TMR-03 | **Applied** | `skills/core/timers-and-async/references/threads-and-runnables.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Core/Public/HAL/Runnable.h:63-72` says a null `GetSingleThreadInterface()` means no single-thread tick; `RunnableThread.h:35-50,76-89` documents `Create` failure as `nullptr` and graceful `Kill`. | Source/static verification only; no single-thread target or runnable shutdown test. Node `v22.23.2`: scoped skills-ref, core citations (5 files, 15 citations), Hermes compatibility, and scoped `git diff --check` all passed. |

### core/umg-and-slate

| Finding | Status | Files | Source evidence | Verification limits |
|---|---|---|---|---|
| UMG-01 | **Applied** | `skills/core/umg-and-slate/SKILL.md`; `references/widget-component-and-input.md` | Installed UE 5.8.2 `Engine/Source/Runtime/Engine/Classes/GameFramework/PlayerController.h:1648-1649` exposes `APlayerController::SetInputMode(const FInputModeDataBase&)`; cursor visibility remains a separate controller property/API. | Source/static verification only; no controller focus, cursor, split-screen, or input-mode runtime test. |
| UMG-02 | **Applied (P1 constructor only)** | `skills/core/umg-and-slate/references/common-widgets-and-layout.md` | Installed UE 5.8.2 `Engine/Source/Runtime/UMG/Public/Components/SlateWrapperTypes.h:155-177` provides the default and one-argument `FSlateChildSize` constructors; the invalid two-argument example was removed. | The audit's P2 hierarchy cleanup was intentionally not applied; no dynamic-widget compile or layout runtime test. |
| UMG-03 | **Applied** | `skills/core/umg-and-slate/references/architecture-and-authoring.md` | Installed UE 5.8.2 `Engine/Plugins/Runtime/ModelViewViewModel/Source/ModelViewViewModel/Public/MVVMViewModelBase.h:16-21,79-90` shows `UE_MVVM_SET_PROPERTY_VALUE` notifies only its specified field and `SetPropertyValue` returns whether it changed, so `SetMaxHealth` now broadcasts `GetHealthPercent` on change. | Source/static verification only; no MVVM plugin compile or binding notification runtime test. |
| UMG-04 | **Applied** | `skills/core/umg-and-slate/SKILL.md`; `references/userwidget-and-binding.md` | Installed `UserWidget.h:510-530` documents design/runtime PreConstruct and Slate lifecycle; `Private/UserWidget.cpp:1219-1248` calls runtime PreConstruct before Construct after Slate widgets exist; `:1858-1882` forwards IsDesignTime. `Public/Slate/SObjectWidget.h:25-30,48-50` makes the live wrapper an FGCObject. Parent corrected lifecycle wording and malformed citation backticks. | Source/static verification only; no widget lifecycle, pooling, GC, or travel runtime test. Scoped metadata/citation checks and final full-tree validation passed. |

### ultra-dynamic-sky/uds-cinematics-rendering

| Finding | Status | Files | Source/vendor evidence | Verification limits |
|---|---|---|---|---|
| UDS-CIN-01 | **Applied** | `skills/ultra-dynamic-sky/uds-cinematics-rendering/SKILL.md` | UDS 9.5 §45 documents `Adjust for Path Tracer` and `Render Height Fog In Path Tracer Using Post Process` as the package compatibility path; installed UE 5.8.2 `Engine/Source/Runtime/Renderer/Private/PathTracing.cpp:428-430,490-492,991-995,1068-1070,2195-2197` exposes native Reference Atmosphere and cloud/path-tracing controls. | UDS/UDW 9.5 assets are not installed here; no package Blueprint/material inspection or MRQ render. Guidance now separates vendor approximation from native engine capability and requires a comparison frame. Node `v22.23.2`: scoped skills-ref, Hermes compatibility, and scoped `git diff --check` passed; vendor citation checker intentionally not run. |

### ultra-dynamic-sky/uds-fog-and-atmosphere

| Finding | Status | Files | Source/vendor evidence | Verification limits |
|---|---|---|---|---|
| UDS-FOG-01 | **Applied** | `skills/ultra-dynamic-sky/uds-fog-and-atmosphere/SKILL.md` | UDS 9.5 §45 states its post-process height-fog approximation; installed UE 5.8.2 `Engine/Source/Runtime/Renderer/Private/PathTracing.cpp:428-430,991-995,1068-1070` exposes native Reference Atmosphere path-tracing selection, while the skill's existing Volumetric Fog section documents the engine-owned fog branch. | UDS/UDW package assets are not installed here; no native/package fog render or visual parity test. Node `v22.23.2`: scoped skills-ref, Hermes compatibility, and scoped `git diff --check` passed; vendor citation checker intentionally not run. |

### ultra-dynamic-sky/uds-setup-and-modes

| Finding | Status | Files | Source/vendor evidence | Verification limits |
|---|---|---|---|---|
| UDS-SET-01 | **Applied** | `skills/ultra-dynamic-sky/uds-setup-and-modes/SKILL.md` | UDS 9.5 §§3,17,73: replace the stock sky stack but allow required external light actors, and back up/use version control plus read the package change log before overwriting assets. | UDS/UDW package is not installed here; no setup, actor/component ownership, version/update, or external-light integration run. Node `v22.23.2`: scoped skills-ref, Hermes compatibility, and scoped `git diff --check` passed; vendor citation checker intentionally not run. |

### ultra-dynamic-sky/uds-time

| Finding | Status | Files | Source/vendor evidence | Verification limits |
|---|---|---|---|---|
| UDS-TIM-01 | **Applied** | `skills/ultra-dynamic-sky/uds-time/SKILL.md` | UDS 9.5 §49 documents obtaining a UDS reference with `Get Ultra Dynamic Sky` and binding Blueprint events such as `Bind to Sunrise`/`Bind to Sunset`; it does not establish the removed native-looking member/signature. | UDS/UDW package assets are not installed here; no Blueprint dispatcher schema or native binding compile/runtime test. Node `v22.23.2`: scoped skills-ref, Hermes compatibility, and scoped `git diff --check` passed; vendor citation checker intentionally not run. |

### ultra-dynamic-weather/udw-material-and-screen-effects

| Finding | Status | Files | Source/vendor evidence | Verification limits |
|---|---|---|---|---|
| UDW-MAT-01 | **Applied** | `skills/ultra-dynamic-weather/udw-material-and-screen-effects/SKILL.md` | UDW 9.5 §105 says `Sample UDW Material State` includes material state and applies Weather Override Volume/Weather Mask effects; §140 identifies Weather Occlusion Volume as a distance-field weather-exposure cache. | UDW package material functions are not installed here; no material graph, volume, or rendered spatial-mask test. Node `v22.23.2`: scoped skills-ref, Hermes compatibility, and scoped `git diff --check` passed; vendor citation checker intentionally not run. |
| UDW-MAT-02 | **Applied** | `skills/ultra-dynamic-weather/udw-material-and-screen-effects/SKILL.md` | UDW 9.5 §101 recommends Rigid for WPO/PDO shadow-cache behavior; installed UE 5.8.2 `Engine/Source/Runtime/Engine/Public/SceneTypes.h:220-229` defines Rigid as suppressing WPO invalidations “use with care,” and `Renderer/Private/VirtualShadowMaps/VirtualShadowMapArray.cpp:200-222` defines the directional/local Nanite tessellation shadow cvars (default 1). | UDW package DLWE graphs are not installed here; no changing-snow shadow comparison, Nanite target render, or performance measurement. |

### ultra-dynamic-weather/udw-setup-and-state

| Finding | Status | Files | Source/vendor evidence | Verification limits |
|---|---|---|---|---|
| UDW-SET-01 | **Applied** | `skills/ultra-dynamic-weather/udw-setup-and-state/SKILL.md` | UDW 9.5 §§84,85,87 document `Change Weather` as the static-preset transition and `Change to Random Weather Variation` as the intentional handoff back to random variation; Manual Weather State is selected by clearing the preset or by calling Change Weather without one. | UDW package is not installed here; no caller/volume/storm/manual-override or random-variation runtime test. Node `v22.23.2`: scoped skills-ref, Hermes compatibility, and scoped `git diff --check` passed; vendor citation checker intentionally not run. |
| UDW-SET-02 | **Applied** | `skills/ultra-dynamic-weather/udw-setup-and-state/SKILL.md` | UDW 9.5 §§84,87,115,116 document Blueprint functions/dispatchers and the Test Actor for Weather Exposure query; the documentation does not establish the removed native-looking `OnStartedRaining.AddDynamic`, `ChangeWeather`, or `TestActorForWeatherExposure` member signatures. | UDW package Blueprint assets and dispatcher signatures are not installed here; no Blueprint node, parameter, or networking/runtime test. Node `v22.23.2`: scoped skills-ref, Hermes compatibility, and scoped `git diff --check` passed; vendor citation checker intentionally not run. |